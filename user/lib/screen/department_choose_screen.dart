import 'package:flutter/material.dart';

class DepartmentChooseScreen extends StatelessWidget {
  const DepartmentChooseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Scaffold(
        body: Container(),
      ),
    );
  }
}
